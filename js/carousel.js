window.addEventListener('load', carouselImg , false)

function carouselImg() {
    let carousel = document.getElementsByClassName('portfolio_block')

    let pos = 0
    for (pos=0; pos < carousel.length; pos++){

        carousel[i].className = carousel[i].className.replace(" active", "");
    }
    carousel.
}